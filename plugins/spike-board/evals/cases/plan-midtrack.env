# plan-midtrack
MARKER_STATE=off
NEEDS=chain
IFS= read -r -d '' PROMPT <<'PROMPT_EOF' || true
Use the spike-board:spike-plan skill. Let's plan my next few spikes.
PROMPT_EOF
IFS= read -r -d '' MUST <<'MUST_EOF' || true
^- Done: 
^- In flight: 
^- Queued: 
Does that match what you expected\?
MUST_EOF
IFS= read -r -d '' MUST_NOT <<'MUST_NOT_EOF' || true
[Oo]perator marker
[Ll]oading
Isaiah|Austin|Andrew
\| # \| Spike \|
⟨|⟩
[Mm]entee machine|[Oo]perator machine|write gate
^```
MUST_NOT_EOF
