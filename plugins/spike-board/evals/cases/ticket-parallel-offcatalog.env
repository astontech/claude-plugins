# ticket-parallel-offcatalog
MARKER_STATE=off
NEEDS=chain
PROMPT=$(cat <<'PROMPT_EOF'
Use the spike-board:spike-ticket skill. Parallel topic that leaves my chain alone: Terraform. I've never used it — I know nothing about it.
PROMPT_EOF
)
MUST=$(cat <<'MUST_EOF'
isn't in the catalog, so these are draft exit questions
drawn from the official docs and a standard curriculum
Which of these could you answer confidently right now\? Give me the numbers\.
^[0-9]\. 
MUST_EOF
)
MUST_NOT=$(cat <<'MUST_NOT_EOF'
[Oo]perator marker
what specifically can't you
[Tt]ell me more about
Isaiah|Austin|Andrew
MUST_NOT_EOF
)
