# ticket-pivot
NEEDS=chain
IFS= read -r -d '' PROMPT <<'PROMPT_EOF' || true
Use the spike-board:spike-ticket skill. Something new that cuts in line: my CI/CD step 3 needs Docker and I've never used Docker at all.
PROMPT_EOF
IFS= read -r -d '' MUST <<'MUST_EOF' || true
The gap, as I understand it: 
Hop check: 
Right\?
MUST_EOF
IFS= read -r -d '' MUST_NOT <<'MUST_NOT_EOF' || true
spike-refine|spike-pivot|spike-plan
\| Ticket \| Disposition \|
Isaiah|Austin|Andrew
⟨|⟩
[Mm]entee machine|[Oo]perator machine|write gate|operator marker
^```
MUST_NOT_EOF
